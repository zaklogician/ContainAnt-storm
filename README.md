# ContainAnt for Apache Storm

A case study using ContainAnt to auto-configure Apache Storm topologies.

## Usage

Place a recent version of SMAC 2.x in `res/smac` and 
execute `res/smac/storm-test.sh`.
